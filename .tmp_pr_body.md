## Summary
- swap the `Package Ladder` section above the `Full 9-package comparison` section on `/packages`
- update the short intro copy in both sections so the order reads naturally

## Testing
- npm run build
- local verification at `http://127.0.0.1:3021/packages?reordersections=1`
