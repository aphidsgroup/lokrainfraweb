## Summary
- remove customer-facing references to source-document language like `v8`, `PDF`, and `taken from`
- keep the Lokra packages and brand-schedule pages written as direct website content

## Testing
- npm run build
- local verification that `/packages` and `/brand-schedule` no longer show `v8` or `PDF`
