## Summary
- fix the Lokra mobile navigation drawer so the menu can scroll properly and the mobile CTA stays visible
- stop relying on inconsistent utility visibility for the desktop/mobile nav split by using explicit responsive nav classes
- add a new `Services Offered` section to `/services` using the owner-provided service hierarchy, without removing the existing service content

## Testing
- npm run build
- local verification of `/services`
- verified desktop header now hides the mobile toggle and keeps the desktop nav visible
